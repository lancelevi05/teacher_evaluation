<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Evaluation Report — {{ $teacher->user->name }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            padding: 40px;
            color: #1F2937;
        }

        h1,
        h2,
        h3 {
            color: #4338CA;
        }

        .header {
            border-bottom: 3px solid #4338CA;
            padding-bottom: 16px;
            margin-bottom: 24px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        th,
        td {
            border: 1px solid #E5E7EB;
            padding: 8px 12px;
            text-align: left;
            font-size: .9rem;
        }

        th {
            background: #EEF2FF;
        }

        @media print {
            .no-print {
                display: none;
            }
        }
    </style>
</head>

<body>
    <div class="no-print text-end mb-3">
        <button class="btn btn-primary" onclick="window.print()">Print / Save as PDF</button>
    </div>

    <div class="header">
        <h1>Teacher Performance Report</h1>
        <p>
            <strong>{{ strtoupper($teacher->user->fname) }}
                {{ strtoupper($teacher->user->lname) }}</strong> · {{ $teacher->user->email }}<br>
            Generated on {{ now()->format('F j, Y') }}
        </p>
    </div>

    <h2>Overall Summary</h2>
    <table>
        <tr>
            <th>Overall Rating</th>
            <td>{{ $stats->a ? number_format($stats->a, 2) : 'No data' }} / 5.00</td>
        </tr>
        <tr>
            <th>Performance Level</th>
            <td>{{ $summary['overall_label'] }}</td>
        </tr>
        <tr>
            <th>Total Evaluations</th>
            <td>{{ $stats->c }}</td>
        </tr>
    </table>

    <h2>Category Breakdown</h2>
    <table>
        <tr>
            <th>Category</th>
            <th>Average Rating</th>
        </tr>
        @forelse ($categories as $c)
            <tr>
                <td>{{ $c->category }}</td>
                <td>{{ number_format($c->avg_rating, 2) }}</td>
            </tr>
        @empty
            <tr>
                <td colspan="2">No data yet</td>
            </tr>
        @endforelse
    </table>

    <h2>Strengths</h2>
    <ul>
        @forelse ($summary['strengths'] as $s)
            <li>{{ $s }}</li>
        @empty
            <li>No standout strengths yet.</li>
        @endforelse
    </ul>

    <h2>Areas for Improvement</h2>
    <ul>
        @forelse ($summary['weaknesses'] as $w)
            <li>{{ $w }}</li>
        @empty
            <li>No significant weaknesses detected.</li>
        @endforelse
    </ul>

    <h2>Recommendations</h2>
    <ul>
        @foreach ($summary['recommendations'] as $r)
            <li>{{ $r }}</li>
        @endforeach
    </ul>

    <p class="subtle" style="margin-top:40px; font-size:.8rem; color:#6B7280;">
        This report was automatically generated by the Teacher Evaluation and Analytics System.
    </p>
</body>

</html>